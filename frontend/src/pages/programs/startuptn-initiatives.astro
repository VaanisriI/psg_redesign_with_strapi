---
import ProgramLayout from '../../components/ProgramLayout.astro';
import { getProgramBySlug } from '../../lib/strapi';

const slug = Astro.url.pathname.split('/').filter(Boolean).pop();
const program = await getProgramBySlug(slug);

const accent = {
  grad: 'linear-gradient(135deg,#6d28d9 0%,#db2777 100%)',
  gradFrom: 'from-purple-700',
  gradTo: 'to-fuchsia-600',
};

const heroImage = 'https://images.unsplash.com/photo-1460925895917-afdab827c52f?q=80&w=2400&auto=format&fit=crop';
---
<ProgramLayout program={program} accent={accent} heroImage={heroImage} />
